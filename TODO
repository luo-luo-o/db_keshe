[x] 在 “消息查询” 界面，点击 “设备总数” “正常设备” “告警设备” “停运设备” 卡片时，显示对应的设备列表
[x] 当 “告警设备” 不为 0 的时候，数值颜色设置为 红色 ， “停运设备” 不为 0 的时候，数值颜色设置为 黄色
[x] ”历史数据” 查询的时候，没有正常数据（疑似数据查询出现问题，因为数据库后台能看到数据）
[x] 前后端 均添加 日志 功能，同时在 ADMIN 登录时，添加一个 “运行日志” 的界面，显示前后端日志，默认级别为 info 

[x] 在查询列表中，同一时间点收集的数据只显示在一行内，并给出一个整体的 “状态” 然后可以点击进去查看对应时间点的各项 具体数据 
[x] 取消 “历史数据” 页的 “最近已有功能” 按钮及相关功能
[x] 添加工单相关界面
[x] 模拟功能失效（写入频率对不上，要求时异常状态每秒写入，但是当前是每3s一个数据。考虑性能问题的原因，如果性能问题，则后续从环境的角度升级。同时前端的 “写入采样” “生成告警” “生成工单” “最近写入” 也要实时能够更新）
[x] 查询相关资料，为传感器数值限定合理范围，超出合理范围则生成工单
[-] 在关闭 start.bat 脚本时，添加关闭 数据库服务器 的命令

[x] 添加“添加设备”功能
[x] 数据库样例数据，更改为使用中文（utf-8）

[x] 添加备份与回溯功能
[x] manager（管理人员）也拥有修改工单的全部权限
[x] 删除 OPERATOR 分组
[x] 开启“模拟测试”的时候，在“模拟测试”的界面，显示模拟测试生成的数据（分页，每页20条，参考“历史数据”界面的表格）
